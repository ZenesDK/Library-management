```markdown
# Управление библиотекой книг

Приложение для управления библиотекой книг с графическим интерфейсом на Avalonia и базой данных SQL Server.

## 📋 Содержание
- [Функциональные возможности](#функциональные-возможности)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и запуск](#установка-и-запуск)
- [Использование приложения](#использование-приложения)
- [Валидация данных](#валидация-данных)
- [Возможные проблемы и решения](#возможные-проблемы-и-решения)
- [Полезные команды](#полезные-команды)
- [Требования к заданию](#требования-к-заданию)
- [Автор](#автор)

## Функциональные возможности

### Основные функции
- 📚 **Управление книгами** (добавление, редактирование, удаление)
- 👤 **Управление авторами** (CRUD операции)
- 🏷️ **Управление жанрами** (CRUD операции)
- 🔍 **Поиск книг по названию**
- 🎯 **Фильтрация книг по автору и жанру**
- ✅ **Валидация ISBN** (поддержка ISBN-10 и ISBN-13)
- 📊 **Отображение количества книг в наличии**

### Особенности реализации
- **Многие-ко-многим**: книга может иметь несколько авторов и несколько жанров
- **Entity Framework Core** для работы с базой данных
- **Avalonia UI** для кроссплатформенного графического интерфейса
- **MVVM паттерн** с использованием ReactiveUI
- **Fluent API** для конфигурации сущностей

## Технологический стек

- **.NET 10.0**
- **Avalonia 11.3.9** (кроссплатформенный UI фреймворк)
- **Entity Framework Core 10.0.3**
- **SQL Server** (в Docker контейнере)
- **ReactiveUI** для MVVM

## Структура проекта

```
kontr1/
├── Models/                 # Модели данных
│   ├── Book.cs
│   ├── Author.cs
│   ├── Genre.cs
│   ├── BookAuthor.cs       # Промежуточная модель для связи книга-автор
│   └── BookGenre.cs        # Промежуточная модель для связи книга-жанр
├── Data/                   
│   └── LibraryContext.cs    # DbContext и настройка Fluent API
├── ViewModels/              # ViewModel для MVVM
│   ├── MainWindowViewModel.cs
│   ├── AuthorsWindowViewModel.cs
│   ├── GenresWindowViewModel.cs
│   ├── BookEditViewModel.cs
│   ├── AuthorEditViewModel.cs
│   └── GenreEditViewModel.cs
├── Views/                    # XAML файлы представлений
│   ├── MainWindow.axaml
│   ├── MainWindow.axaml.cs
│   ├── AuthorsWindow.axaml
│   ├── AuthorsWindow.axaml.cs
│   ├── GenresWindow.axaml
│   ├── GenresWindow.axaml.cs
│   ├── BookEditWindow.axaml
│   ├── BookEditWindow.axaml.cs
│   ├── AuthorEditWindow.axaml
│   ├── AuthorEditWindow.axaml.cs
│   ├── GenreEditWindow.axaml
│   └── GenreEditWindow.axaml.cs
├── App.axaml                 # Конфигурация приложения
├── App.axaml.cs
├── Program.cs                # Точка входа
└── kontr1.csproj             # Файл проекта
```

## Установка и запуск

### Предварительные требования
- **.NET 10.0 SDK** или новее
- **Docker** (для запуска SQL Server)
- **Git** (опционально, для клонирования)

Проверьте установленные версии:
```bash
dotnet --version
docker --version
```

### 1. Клонирование репозитория
```bash
git clone <url-репозитория>
cd kontr1
```

### 2. Запуск SQL Server в Docker

Запустите контейнер с SQL Server:
```bash
sudo docker run -e "ACCEPT_EULA=Y" \
                -e "MSSQL_SA_PASSWORD=YourStrongPassword123" \
                -p 1433:1433 \
                --name sqlserver \
                -d mcr.microsoft.com/mssql/server:2022-latest
```

Если контейнер уже существует:
```bash
sudo docker start sqlserver
```

Проверьте, что контейнер работает:
```bash
sudo docker ps
```

### 3. Настройка строки подключения

В файле `Data/LibraryContext.cs` убедитесь, что строка подключения корректна:
```csharp
optionsBuilder.UseSqlServer("Server=localhost;Database=LibraryDB;User Id=SA;Password=YourStrongPassword123;TrustServerCertificate=True;");
```

### 4. Сборка и запуск приложения

```bash
# Перейдите в папку проекта
cd kontr1

# Очистите проект
dotnet clean
rm -rf obj bin

# Восстановите пакеты
dotnet restore

# Соберите проект
dotnet build

# Запустите приложение
dotnet run
```

### 5. Создание базы данных

При первом запуске база данных создастся автоматически благодаря `Database.EnsureCreated()`.

Если нужно пересоздать базу данных (после изменения моделей):
```bash
# Подключитесь к SQL Server
sqlcmd -S localhost -U SA -P 'YourStrongPassword123'

# Выполните SQL-запросы
DROP DATABASE IF EXISTS LibraryDB;
GO
CREATE DATABASE LibraryDB;
GO
EXIT;
```

## Использование приложения

### Главное окно
- **Список книг** отображается в центре
- **Поиск по названию** — текстовое поле в верхней панели
- **Фильтр по автору** — выпадающий список
- **Фильтр по жанру** — выпадающий список
- Кнопка **"Сбросить"** — сбрасывает все фильтры
- Кнопка **"Обновить"** — принудительно обновляет данные

### Управление книгами
1. **Добавить книгу** — кнопка "Добавить книгу"
   - Заполните название (обязательно)
   - Выберите авторов (удерживайте Ctrl для множественного выбора)
   - Выберите жанры (удерживайте Ctrl для множественного выбора)
   - Укажите год издания
   - Введите ISBN (валидация формата)
   - Укажите количество в наличии
   - Нажмите "Сохранить"

2. **Редактировать книгу** — выберите книгу в списке, нажмите "Редактировать"

3. **Удалить книгу** — выберите книгу, нажмите "Удалить"

### Управление авторами
- Кнопка **"Авторы"** открывает окно управления
- Добавление, редактирование, удаление авторов

### Управление жанрами
- Кнопка **"Жанры"** открывает окно управления
- Добавление, редактирование, удаление жанров

## Валидация данных

### ISBN
- Поддерживаются ISBN-10 и ISBN-13
- Дефисы и пробелы игнорируются
- Для ISBN-10 последний символ может быть 'X'

### Год издания
- Диапазон: от 1800 до текущего года
- Обязательное поле

### Количество в наличии
- Должно быть неотрицательным числом
- Обязательное поле

## Возможные проблемы и решения

### Ошибка подключения к SQL Server
```
System.Data.SqlClient.SqlException: A network-related or instance-specific error
```
**Решение:** Запустите контейнер Docker:
```bash
sudo docker start sqlserver
```

### Ошибка "Invalid object name 'BookAuthors'"
**Решение:** Пересоздайте базу данных:
```bash
sqlcmd -S localhost -U SA -P 'YourStrongPassword123'
DROP DATABASE IF EXISTS LibraryDB;
GO
CREATE DATABASE LibraryDB;
GO
EXIT;
```

### Ошибка сборки "CS0246: Не удалось найти тип или имя пространства имен"
**Решение:** Проверьте, что все файлы моделей находятся в папке `Models` и имеют правильные `using`.

### Книги не отображаются в главном окне
**Решение:** Нажмите кнопку **"Обновить"** или проверьте консоль на наличие ошибок.

### Конфликт имени контейнера Docker
```
docker: Error response from daemon: Conflict. The container name "/sqlserver" is already in use
```
**Решение:** Используйте существующий контейнер:
```bash
sudo docker start sqlserver
```

## Полезные команды

### Управление Docker контейнером
```bash
# Запуск контейнера
sudo docker start sqlserver

# Остановка контейнера
sudo docker stop sqlserver

# Просмотр логов
sudo docker logs sqlserver --tail 50

# Удаление контейнера
sudo docker rm sqlserver
```

### Работа с базой данных
```bash
# Подключение к SQL Server
sqlcmd -S localhost -U SA -P 'YourStrongPassword123'

# Просмотр таблиц
USE LibraryDB;
GO
SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES;
GO

# Просмотр данных
SELECT * FROM Books;
SELECT * FROM Authors;
SELECT * FROM Genres;
```

### Пересборка проекта
```bash
dotnet clean
rm -rf obj bin
dotnet restore
dotnet build
dotnet run
```

### Миграции Entity Framework
```bash
# Создание миграции
dotnet ef migrations add MigrationName

# Применение миграции
dotnet ef database update
```

## Требования к заданию (выполнено)

- [x] Создание сущностей Book, Author, Genre
- [x] Настройка связей через Fluent API
- [x] Первичные ключи, обязательные поля, ограничения длины
- [x] Каскадное удаление
- [x] Avalonia интерфейс (вместо WPF)
- [x] DataGrid для списка книг
- [x] Панель фильтрации по жанру и автору
- [x] CRUD операции для всех сущностей
- [x] Поиск книг по названию
- [x] Отображение количества книг в наличии
- [x] Отдельные окна для управления авторами и жанрами
- [x] Валидация ISBN
- [x] Множественные авторы и жанры для книг

## Автор

**Студент:** [Ваше имя]  
**Группа:** [Ваша группа]  
**Дисциплина:** Программирование корпоративных систем  
**Преподаватель:** Яковлев Михаил Сергеевич  
**Институт:** Институт перспективных технологий и индустриального программирования  
**Кафедра:** Индустриального программирования  
**Семестр:** 2 семестр, 2025-2026 гг.

---

© 2026 Учебный проект. Все права защищены.
```

## Как использовать

1. Создайте файл `README.md` в корневой папке проекта
2. Скопируйте всё содержимое выше
3. Замените `[Ваше имя]` и `[Ваша группа]` на свои данные
4. Сохраните файл

Файл готов к использованию и содержит всю необходимую информацию для запуска и работы с проектом.